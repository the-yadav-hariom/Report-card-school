server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/school_report_card_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# Spring Data JPA & Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration Secret & Expiration (24 Hours)
school.app.jwtSecret=SchoolReportCardSecretKeySuperSecureJwtToken2024StitchAcademia
school.app.jwtExpirationMs=86400000
